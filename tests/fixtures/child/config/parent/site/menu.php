<?php

// PARTIAL override. Sets only type.mobile — type.desktop must survive, and the
// whole `effect` block (absent here, as if commented out) must be inherited.
return [
    'type' => [
        'mobile' => 'fullscreen',
    ],
];
